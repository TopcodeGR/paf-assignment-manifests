apiVersion: v1
kind: Service
metadata:
  name: paf-assignment-db-service
  namespace: paf-assignment
  labels:
    app: paf-assignment-db-deployment
spec:
  type: NodePort
  ports:
    - name: psql-port
      port: 5432
      targetPort: 5432
  selector:
    app: paf-assignment-db-deployment

