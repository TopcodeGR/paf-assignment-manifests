apiVersion: v1
kind: Service
metadata:
  name: paf-assignment-service
  namespace: paf-assignment
spec:
  ports:
    - protocol: TCP
      name: paf-assignment-service
      port: 8080
  selector:
    app: paf-assignment-deployment

